#include <bits/stdc++.h>
using namespace std;

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
typedef long long ll;

int main(){
    int n; cin >> n;
    vector<ll> v; v.resize(n);
    for(int i = 0; i < n; i++){
        cin >> v[i];
        
    
    }


    return 0;
}