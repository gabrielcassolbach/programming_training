#include <bits/stdc++.h>
using namespace std;

#define fastio ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

typedef long long ll;

int solve(ll n) {
	if(n == 0) return 0;
	
	
}


int main(){
   	fastio;
	ll n; cin >> n;
	solve(n);
    return 0;
}
