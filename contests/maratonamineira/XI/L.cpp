#include <bits/stdc++.h>
using namespace std;

#define _ ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);

int n, q;
vector<int> v;

void read(){
	cin >> n >> q;
	v.resize(n + 10);
	for(int i = 2; i <= n; i++)
		cin >> v[i];
}

int main(){
	read();
	

	return 0;
}
