#include "bits/stdc++.h"
using namespace std;

int main(){
    int n, a, b, c; bool sol;
    scanf("%d", &n);
    while(n--){
        scanf("%d %d %d", &a, &b, &c); sol = 0;
        // Retornar nesta questão.
    }

    return 0;
}