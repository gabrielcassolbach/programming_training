#include <bits/stdc++.h>
using namespace std;
#define MAX 20 

int t, n;

/*
Caso base:
    if(found) return.
    else if(sum > n) return.
    else if(sum == n && !found) print the solution.
*/

void backtrack(int c){
    

    for(int i = c; i < n; i++){
        


    }

}

int main(){
    while(scanf("%d", &t) != EOF){ 
        scanf("%d", &n); 
        
    } 
    return 0;
}   